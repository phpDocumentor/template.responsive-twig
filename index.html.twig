{% extends 'layout.html.twig' %}

{% block content %}
        <div class="hero-unit">
            <h1>{{ project.name|raw }}</h1>
            <h2>Documentation</h2>
        </div>

        <div class="row">
            <div class="span7">
                {% if project.namespace.namespaces.count > 0 or not project.index.package %}
                    <div class="well">
                        <ul class="nav nav-list">
                            <li class="nav-header">Namespaces</li>
                            <li><a href="{{ path(project.namespace) }}">Global ({{ project.namespace.name }})</a></li>
                            {% for namespace in project.namespace.namespaces %}
                                <li><a href="{{ path(namespace) }}">{{ namespace.name }}</a></li>
                            {% endfor %}
                        </ul>
                    </div>
                {% endif %}

                {% if project.index.package %}
                    <div class="well">
                        <ul class="nav nav-list">
                            <li class="nav-header">Packages</li>
                        </ul>
                    </div>
                {% endif %}

            </div>
            <div class="span5">
                <div class="well">
                    <ul class="nav nav-list">
                        <li class="nav-header">Charts</li>
                        <li>
                            <a href="{{ path('graph_class.html') }}">
                                <i class="icon-list-alt"></i> Class inheritance diagram
                            </a>
                        </li>
                    </ul>
                </div>
                <div class="well">
                    <ul class="nav nav-list">
                        <li class="nav-header">Reports</li>
                        <li><a href="{{ path('errors.html') }}"><i class="icon-list-alt"></i> Errors</a></li>
                        <li><a href="{{ path('markers.html') }}"><i class="icon-list-alt"></i> Markers</a></li>
                    </ul>
                </div>
            </div>
        </div>
{% endblock %}